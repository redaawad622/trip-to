import Vue from 'vue';
import Vuex from 'vuex';
import ResortsModule from './Modules/ResortsModule';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: { ResortsModule },
});
